"""
helpers.py — General-purpose utility functions.

TODO: Implement the following functions:
    - numpy_to_pil(img): Convert a NumPy array to a PIL Image.
    - pil_to_numpy(img): Convert a PIL Image to a NumPy array.
    - ensure_rgb(img): Convert BGR → RGB if needed.
"""
